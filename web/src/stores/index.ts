import { configureStore } from '@reduxjs/toolkit';

// import loginUserReducer from "./ducks/loginUser";

export default configureStore({
  reducer: {
    // loginUser: loginUserReducer
  },
});
