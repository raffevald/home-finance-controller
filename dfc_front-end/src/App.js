import { useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { getAuthLoginFetch } from './store/reducer/authLogin'

import './App.css';

function App() {
  // const dispatch = useDispatch();

  // const user = useSelector(state => state.authLogin.authLogin);

  // useEffect(() => {
  //   dispatch(getAuthLoginFetch());
  // },[dispatch]);
  // console.log(user);



  return (
    <div className="App">

    </div>
  );
}

export default App;
